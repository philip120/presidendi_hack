Drop demo equation or worksheet images in this folder.

Then edit `HARDCODED_IMAGE_PATHS` in `../math_agent.py`, for example:

```python
HARDCODED_IMAGE_PATHS = [
    "./images/equation_photo.jpg",
]
```
